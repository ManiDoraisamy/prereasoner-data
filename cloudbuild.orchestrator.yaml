# Build + push the orchestrator (Sonnet chat backend + PreReasoner MCP server) image, GATED on tests.
#
#   gcloud builds submit --config cloudbuild.orchestrator.yaml \
#     --substitutions=_REGION=us-central1,_REPO=prereasoner,_TAG=latest
#
# The `test` step runs the full suite (tests/run_all.py) FIRST — "test all of them every deployment".
# RUN_ENGINE_TESTS=0 skips the live-Postgres engine suites (no world DB in the build sandbox); test_mcp
# always runs, and test_orchestrator self-skips unless ANTHROPIC_API_KEY is provided (wire it via a
# Secret Manager substitution to exercise routing discipline in the gate too). A failing suite fails
# the build, so a broken orchestrator never ships.

steps:
  - id: test
    name: python:3.11
    entrypoint: bash
    args:
      - -c
      - |
        set -e
        pip install --no-cache-dir -r orchestrator/requirements.txt
        RUN_ENGINE_TESTS=0 python -m tests.run_all

  - id: build
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - Dockerfile.orchestrator
      - -t
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/chat:${_TAG}
      - .

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/chat:${_TAG}

substitutions:
  _REGION: us-central1   # must match infra/ var.region
  _REPO: prereasoner     # must match infra/ var.artifact_repo
  _TAG: latest

timeout: 900s
