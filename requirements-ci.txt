# Lightweight dependencies for hermetic tests and static checks.
# The model stack in requirements.txt is required only for live engine/model suites.
httpx>=0.27,<1
mcp==1.29.1
numpy==1.26.4
scikit-learn==1.9.0
psycopg2-binary>=2.9,<3
sqlalchemy>=2.0,<3                # TEST ONLY: hydrates the emitter-parity oracle's object
                                  # graph. Never add to requirements.txt — the Python
                                  # emitter must never be importable from serving.
requests>=2.31,<3
defusedxml>=0.7,<1
bandit==1.9.4
pip-audit==2.10.1
ruff==0.16.3
