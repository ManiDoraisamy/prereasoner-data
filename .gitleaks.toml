title = "Prereasoner secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Firebase and Picker browser identifiers are public client configuration; server authorization never trusts them."
paths = ['''^web/public/lib/config\.js$''']
