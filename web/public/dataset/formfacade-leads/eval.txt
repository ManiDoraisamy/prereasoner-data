# Follow-up evaluation for this demo workbook.
# Each line: [chat:] <follow-up question> => <expected scalar>
# Sent IN ORDER, in the SAME conversation, after prompt.txt has been answered.
# Expected values are derived from the CSVs independently of the engine (docs/TESTING.md);
# `~` = compare within the FX drift tolerance (the ECB rate moves daily).
# `chat:` = conversational SHORTHAND that only the orchestrated path can resolve. The direct
#           engine reads wording literally, so tests.test_datasets skips these; the Chrome
#           release pass (which drives the real product) is what verifies them.
# ORDER IS PART OF THE CONTRACT: the euros claim lands on the CURRENT scope (Germany, set by
# the line before it), NOT on the prompt's Europe scope. The expectation here was first
# written as the Europe figure; the Chrome pass caught it.
total budget in Germany => 33000
# The country-scoped turn carrying the EUR claim: the case that returned an EMPTY value until
# 2026-09-08, because a synthesized column was world-routed. See DECISIONS.md.
chat: This is in euros. Whats in USD => ~38352.60
# An aggregate over zero matching rows must say so, never answer with a blank.
total budget in Africa => clarify
# The claim must SURVIVE a widening of scope, and still convert.
chat: How about all of Europe? => ~72056.40
